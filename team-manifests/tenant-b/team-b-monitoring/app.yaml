apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-b-monitoring
spec:
  selector:
    matchLabels:
      app: team-b-monitoring
  template:
    metadata:
      labels:
        app: team-b-monitoring
    spec:
      containers:
        - name: team-b-monitoring
          image: paulbouwer/hello-kubernetes:1.10
          ports:
            - name: http
              containerPort: 5000
          resources:
            requests:
              memory: 32Mi
              cpu: 50m
            limits:
              memory: 128Mi
              cpu: 500m
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team-b-monitoring
spec:
  rules:
    - host: team-b-monitoring.local.mikesir87.training
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: team-b-monitoring
              port: 
                number: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: team-b-monitoring
spec:
  selector:
    app: team-b-monitoring
  ports:
    - port: 5000
